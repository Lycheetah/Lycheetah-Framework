# Π — THE 2026-08-02 FORGE, AND WHAT IT ACTUALLY MEASURED

**Status:** MEASURED where marked, 2026-08-03. Codex-authored ingestion note.
**Register:** this is an INGESTION AND VERIFICATION REPORT. It does not amend the
canon. Where the forge diverges from `TRUTH_PRESSURE_CANON.md`, the canon is the
theory and the forge is a fact about programs that were run.

**Where it lives:**

| path | what |
|---|---|
| `FORGE_2026-08-02/` | the complete bundle exactly as shipped. **AUTHORITATIVE.** 29 files, `sha256sum -c SHA256_MANIFEST.txt` → **29 OK at this path.** |
| `FORGE_2026-08-02_EXPANDED/` | ⚠ **NON-AUTHORITATIVE CONVENIENCE EXPANSION.** 214 files, generated by unzipping the nine archives in `FORGE_2026-08-02/`. It exists so the sources can be read and grepped. **The zips are the truth.** If the two ever disagree, delete the expansion and re-unzip — never edit it and never cite it as a separate artifact. |

---

## ⚠⚠ THE Π-WAS-A-CONSTANT DEFECT: WHERE IT IS, AND WHY THAT IS CORRECT

`APP_IMPLEMENTATION_STATE_2026-08-01.md` records the defect where Π was
effectively a constant and *"a comment reciting a formula is not an
implementation of it."* Anyone grepping this forge **will hit that defective code
and must not misread it as live.**

**MEASURED — I checked every Π site in all nine archives:**

```
FOUND, and CORRECT to be there — the historical baseline, deliberately preserved:
  FORGE…_EXPANDED/TRUTH_PRESSURE_SOURCE_AUDIT_PASS_1_v0.1/SOURCE_SNAPSHOT/
    lib/cascade-score.ts:125   rawPi = evidencePower / (100 / coherenceDiv)

FOUND, and CORRECT to be there — quoted INSIDE A BLOCK COMMENT that explains the repair:
  …_v0.6/lib/cascade-score.ts:125     (comment opens ~line 117, closes ~line 149)
  …_v0.7/lib/cascade-score.ts:125

LIVE CODE in those same files, line 166:
  const truthPressure = parseFloat(((E * P * S0) / (S + S0)).toFixed(3));
```

⭐ **The trap is exact and worth naming: the defective line and the correct line
sit 41 lines apart in the same file, and a grep shows both.** Line 125 is prose
about a repair. Line 166 is the repair. Read the delimiters, not the hit.

### The engines compute Π, and I ran them to prove it

```
ENGINE v0.2  src/core.ts:40   … / (components.strain + config.s0)
ENGINE v0.3  src/core.ts:45   piCanon = (evidence · explanatoryPower) / (strain + s0)
                              piNormalized = piCanon × s0
```

**MEASURED on this machine, 2026-08-03, Node v24.16.0** — v0.3's own gate and an
independent hand-computation of `(E·P)/(S+S₀)`:

```
node --experimental-strip-types scripts/verify.ts   →  🟢 24 checks passed

E=1    P=1    S=0.001   piCanon 19.608   hand 19.608   ✓   piNorm 0.980
E=1    P=1    S=0.05    piCanon 10.000   hand 10.000   ✓   piNorm 0.500
E=0.8  P=0.6  S=0.2     piCanon  1.920   hand  1.920   ✓   piNorm 0.096
E=0.5  P=0.5  S=0.5     piCanon  0.455   hand  0.455   ✓   piNorm 0.023
```

Agreement is exact to 1e-12, and these are the **same four rows** already recorded
as MEASURED in `APP_IMPLEMENTATION_STATE_2026-08-01.md`. The ×20 canon↔app scale
relation documented there holds here: v0.3 exposes both `piCanon` and
`piNormalized = piCanon × s₀` explicitly, and names the conversion in its own
output. **The scale ambiguity that document warned about is closed inside the
engine, not left to the reader.**

⚠ Verdict on the standing warning: **the incoming engines genuinely implement Π.**
The constant-Π defect survives only as preserved history.

---

## ✅ THE REAL FINDING: THE INSTRUMENT FAILED ITS OWN FROZEN CORPUS

This is the most important thing in the forge, and it is a **negative result**.

`02_AUDIT/…PASS_1_v0.1` ran the untouched `scoreCASCADE` against 24 preregistered
controlled cases. **I read the raw results file directly, not the summary:**

```
TP_UNTUNED_TEXT_RESULTS_v0.1.jsonl     24 rows
truth_pressure == 0                    24 / 24     MEASURED
reorganisation_needed == true           0 / 24     MEASURED
```

Against that, the audit's own failure matrix records (`status: MEASURED`) that

> "By definition this proven theorem must be true because therefore the framework
> explains the pattern."

scored **Π = 0.831** — pure unsupported certainty — while a plain preregistered
prediction with a measured result scored **Π = 0**.

The audit states the cause plainly: `P` is operationalised from certainty markers
(`proven`, `by definition`, `theorem`, `must be`), which are **not** explanatory
reach. Its own verdict:

```
GENERAL-LANGUAGE CONSTRUCT VALIDITY      FAILED
MARKER-GAMING RESISTANCE                 FAILED
LAYER LENGTH INVARIANCE                  FAILED
SEMANTIC POLARITY CONSISTENCY            FAILED
UNIFIED REORGANISATION SEMANTICS         FAILED
PARTIAL-JUDGE-OUTPUT SAFETY              FAILED
```

⭐ **A gate can pass while the construct fails.** The shipped six-fixture gate
exits 0 and its checks are real — but the fixtures are the instrument's own
authored marker families. Passing them showed the *scalar* was repaired; the
24-case corpus showed the *construct* was not. Both are true at once, and only
the second one is about evidence.

⚠ The audit also finds a **fourth Π** (`computePyramidPi`: E = mean file score,
P = max file score, S = score spread). With canon Π, text-lens Π and onion Π,
that is four different constructs wearing one symbol.
`APP_IMPLEMENTATION_STATE_2026-08-01.md` documented three. **This makes it four.**

---

## ⚠ WHAT IS STILL ONLY ASSERTED

**There is no human data and no external validation anywhere in this forge.**
MEASURED by inspection of `04_MEASUREMENT/…MEASUREMENT_PACK_v0.4`:

- `rater_packets/` holds three **blank** packets (A, B, C) — materials, not responses;
- `reports/` holds exactly two files: `PRE_REGISTRATION.md`, whose own status line
  reads *"Frozen before human ratings are collected"*, and
  `COLLECTION_CHECKLIST.md`, in which **every one of the nine boxes is unticked**;
- `scripts/analyze_annotations.py` exists and has nothing to analyse.

That is a preregistration done properly — frozen before collection, blinding key
sealed, outcomes named in advance. **It is not a result.** Krippendorff alpha,
rater agreement and engine correspondence are all *planned*, none computed.

`00_INDEX.md` in the bundle lists eight items under **"Still required"**,
including *"collect blinded human ratings"*, *"calibrate only after
measurement"*, and *"perform held-out validation before scientific claims."*
The bundle does not overclaim; nothing in it needed downgrading.

### Honest epistemic position after this ingest

```
Π implements the canonical formula                 MEASURED (re-executed here)
engine v0.3 passes its 24 hardening checks          MEASURED (re-executed here)
the text lens fails general-language validity       MEASURED (24/24 Π=0)
marker-stuffing beats measured evidence             MEASURED (0.831 vs 0)
E/P/S mean the same to independent humans           NOT TESTED — no ratings exist
Π tracks anything outside this instrument           NOT TESTED — no external data
thresholds (Π>0.6, k∈[0.8,1.5]) are calibrated      NO — still chosen, not fitted
```

The prior corpus audit's shape — **strong formal work, no empirical data** —
still holds for the *theory*. What changed on 2026-08-02 is narrower and real:
there is now **executed measurement of the instrument against itself**, and it
returned a failure the preregistration was built to expose. That is genuine
methodological progress. It is not evidence about the world.

⚠ A repository describing its own success is not independent validation — and
that cuts both ways here. This forge mostly describes its own *failure*, which
is more credible than praise, but it is still the same author measuring the same
instrument on a corpus he wrote.

---

## Provenance

Sources, ingested 2026-08-03:
`~/Downloads/TRUTH_PRESSURE_COMPLETE_FORGE_2026-08-02/` and
`~/Downloads/TRUTH_PRESSURE_LIVE_SOURCE_COLLECTOR_v0.8/` (the latter **byte-identical**
to the copy already inside the forge at `06_TOOLS/`, so it was not imported twice).

Two of the forge's 29 files — `00_FOUNDATION/TRUTH_PRESSURE_CANON.md` and
`00_FOUNDATION/TRUTH_PRESSURE_THEORY.md` — are byte-identical to the copies
already at the root of this folder. They were kept inside the bundle so its
manifest still verifies; **the root copies remain the ones to cite.**

Full receipt: `../13_SYNTHESIS_REPORTS/INGEST_2026-08-03_LAMAGUE_AND_TRUTH_PRESSURE.md`.
