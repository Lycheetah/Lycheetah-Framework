name: Framework Health Check

on:
  push:
    branches: [main, master, dev]
  pull_request:
    branches: [main, master, dev]

jobs:
  health:
    runs-on: ubuntu-latest
    name: Alexandria Health & Drift Audit
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy scipy networkx

      # Scripts live under 12_IMPLEMENTATIONS/ (moved off repo root).
      - name: Run Alexandria health check
        run: python 12_IMPLEMENTATIONS/alexandria_agent.py health
        env:
          PYTHONPATH: ${{ github.workspace }}/12_IMPLEMENTATIONS

      - name: Run Alexandria drift audit
        run: python 12_IMPLEMENTATIONS/alexandria_agent.py drift
        env:
          PYTHONPATH: ${{ github.workspace }}/12_IMPLEMENTATIONS

      - name: Run Alexandria gap report
        run: python 12_IMPLEMENTATIONS/alexandria_agent.py gaps
        env:
          PYTHONPATH: ${{ github.workspace }}/12_IMPLEMENTATIONS

      - name: Report status
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "CI WORKFLOW COMPLETE"
          echo "Framework health monitored by Alexandria Agent"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
