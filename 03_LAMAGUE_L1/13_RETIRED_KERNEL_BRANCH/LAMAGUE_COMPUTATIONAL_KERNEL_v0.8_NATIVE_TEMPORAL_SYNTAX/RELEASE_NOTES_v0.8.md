# Release Notes — v0.8.0

## Added

- native temporal `.lmg` syntax;
- statement AST with source hash and raw statements;
- provenance compiler for observed, declared, calculated, and inferred evidence;
- native unknown and conflict declarations;
- native drift dimensions, intent, action, recovery horizon, and invariants;
- preserve-target validation;
- deterministic compilation to the v0.7 temporal packet schema;
- 12-case native adversarial benchmark;
- canon and version map.

## Preserved

- all v0.6 semantic benchmark assets;
- all v0.7 temporal bridge assets and calculations;
- 66 inherited tests;
- existing `lamague_kernel` and `lamague_temporal` interfaces.

## Claim boundary

No independent validation is claimed. Passing tests establish implementation behavior only.
