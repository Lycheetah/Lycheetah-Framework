# Source Lineage and Architectural Position

This milestone builds on two already-separated LAMAGUE tracks.

## Executable TRIAD substrate

`LAMAGUE_EXECUTABLE_KERNEL_v0.1` established:

```text
tokenization
parser
typed AST
canonical serialization
deterministic state transformations
traceable execution
```

## Consequential semantic packet lineage

The wider computational work established the Public Core:

```text
O Observe
E Evidence
U Unknown
I Invariant
G Guard
V Vector Invert
F Fold
Y Yield
Z Compress / Expand
```

It also established the protected semantic fields used here:

```text
evidence
unknowns
invariants
authority
affected parties
dissent
value flow
recovery
lineage
```

## What this milestone adds

```text
a reversible compact wire format
a training-only shared codebook
an untouched held-out split
byte-level compression accounting
full and critical hash receipts
constructed unsafe-collapse attacks
an article-ready evidence ledger
```

The TRIAD runtime and semantic packet codec remain distinct modules. This
package does not silently merge their different purposes.
