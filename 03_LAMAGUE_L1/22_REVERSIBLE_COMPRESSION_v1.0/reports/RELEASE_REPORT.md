# Release Report

## Verification

```text
Unit test exit code: 0
Benchmark exit code: 0
```

## Article threshold reached

The package now contains:

- a frozen diverse structured corpus;
- a training-only exact-string dictionary;
- an untouched held-out split;
- a reversible `L1` / `L1D` codec;
- exact full and critical hash receipts;
- byte accounting with codebook cost;
- 324 constructed semantic-loss attacks;
- a measured report;
- a post-ready article draft;
- a static browser evidence viewer.

## Measured limits

All results are internal deterministic software measurements on a synthetic
structured corpus. No external human or model decoder evidence has been added.
