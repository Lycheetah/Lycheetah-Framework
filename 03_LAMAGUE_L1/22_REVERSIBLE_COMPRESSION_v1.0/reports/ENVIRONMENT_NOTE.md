# Environment Note

The user-visible Python environment emitted an unrelated spreadsheet-runtime warmup warning before the first test run. It did not affect this package. The test process exited with code 0, and this clean report contains only the LAMAGUE unit-test output.
