# LAMAGUE‑1C v1.0 — The One‑Cell Power Language

## Definition

LAMAGUE‑1C is the usable single-cell layer of LAMAGUE Native‑36.

```text
36 primitive cells
=
10 fused numeral cells
+
26 typed semantic-operation cells
```

Repeated semantic programmes may be fused into **power seals**:

```text
one visible seal
⇄ ordered primitive sequence
⇄ typed AST
⇄ executable operation
⇄ natural-language recovery
```

## The 36 equivalents

```text
0 Null        1 Unit         2 Difference   3 Three
4 Four        5 Five         6 Six          7 Seven
8 Eight       9 Nine

A Anchor      B Bridge       C Cycle        D Drift
E Evidence    F Fold         G Guard        H Horizon
I Invariant   J Junction     K Kernel       L Limit
M Merge       N Novelty      O Observe      P Prove
Q Query       R Resolve      S State        T Transition
U Unknown     V VectorInvert W Weave        X Exchange
Y Yield       Z Compress
```

ASCII is the permanent transport alias. The native SVG cell is the visual body.

## Five power seals

### Correction

```text
COR ⇄ D → A → F → P → Y
```

Detect drift, anchor to the declared baseline, integrate history, prove continuity, then yield.

### Frontier

```text
FRO ⇄ O → S → N → E → G → I → U → Q
```

Observe a claim, form its state, measure novelty and evidence, guard the foundation, preserve invariants and uncertainty, then query what is required.

### Sovereignty

```text
SOV ⇄ G → I → V → P → Y
```

Guard authority, preserve invariants, invert unsafe paths, prove the result, then yield.

### Cascade

```text
CAS ⇄ E → N → J → C → F → P → Y
```

Evidence and novelty reach a junction, initiate bounded reorganisation, fold history, prove continuity, then yield.

### Protected Dissent

```text
DIS ⇄ W → U → G → V → Y
```

Remain connected, preserve unresolved disagreement, guard identity, create an alternative path, then yield.

## Core demonstration

```text
FRO → COR
```

is two visible cells, reversibly expanding into:

```text
O → S → N → E → G → I → U → Q
→ D → A → F → P → Y
```

That is:

```text
13 typed operations → 2 visible programme cells
```

Meaning:

> Observe a frontier claim without collapsing uncertainty. Protect the invariant foundation. Then detect any resulting drift, re-anchor, integrate relevant history, prove continuity, and emit the corrected state.

## Syntax

```text
A → F       sequence
E & N       parallel measurement
G[I] : T    guarded transition
U<Cause>    typed unknown
D@7         drift strength band seven
C^3         repeat cycle three times
SEAL[COR]   explicit correction seal
```

## Hard law

A seal is valid only when its expansion is:

- exact
- ordered
- typed
- inspectable
- executable
- recoverable

A one-cell mark that cannot be decompressed is not LAMAGUE compression. It is opacity.
